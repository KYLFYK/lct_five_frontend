import Space from 'antd/lib/space';import Typography from 'antd/lib/typography';import { FC } from 'react';import { TTestBaseSettings } from '../../types/tests';import { nodeComponentByType } from '../../utils/testing-utils';import { TestingWrapper } from './testing-preview';type TProps = {  descriptionFull: TTestBaseSettings['descriptionFull'];};export const TestingFullDescription: FC<TProps> = ({ descriptionFull }) => {  return (    <TestingWrapper>      {descriptionFull.length > 0 ? (        <Space direction="vertical" size={12}>          {descriptionFull            .sort((a, b) => {              console.log(a.order, b.order);              if (a.order === b.order) {                return 0;              }              if (a.order === undefined) {                return 1;              } else if (b.order === undefined) {                return -1;              }              return a.order - b.order;            })            .map((node) => (              <div key={node.key}>{nodeComponentByType(node)}</div>            ))}        </Space>      ) : (        <Typography.Title level={3} disabled>          Описание отсутствует        </Typography.Title>      )}    </TestingWrapper>  );};