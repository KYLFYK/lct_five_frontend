import { Empty } from 'antd';import Space from 'antd/lib/space';import { FC } from 'react';import { TTestBaseSettings } from '../../types/tests';import { sortByOptionalOrder } from '../../utils/common';import { nodeComponentByType } from '../../utils/testing-utils';import { TestingWrapper } from './testing-preview';type TProps = {  descriptionFull: TTestBaseSettings['descriptionFull'];  fullPage?: boolean;};export const TestingFullDescription: FC<TProps> = ({  descriptionFull,  fullPage = true,}) => {  return (    <TestingWrapper className={fullPage ? 'full-page' : ''}>      {descriptionFull.length > 0 ? (        <Space direction="vertical" size={12}>          {descriptionFull.sort(sortByOptionalOrder).map((node) => (            <div key={node.key}>{nodeComponentByType(node)}</div>          ))}        </Space>      ) : (        <Empty          image={Empty.PRESENTED_IMAGE_SIMPLE}          description="Описание отсутствует"        />      )}    </TestingWrapper>  );};