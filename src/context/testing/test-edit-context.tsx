import {  Dispatch,  FC,  PropsWithChildren,  SetStateAction,  createContext,  useContext,  useState,} from 'react';import { mockTesting } from '../../mock/mockTesting';import { TQuestion, TTestBaseSettings, TTestStage } from '../../types/tests';type TTestEditContext = {  baseSettings: TTestBaseSettings;  editBaseSettings: Dispatch<SetStateAction<TTestBaseSettings>>;  stages: TTestStage[];  editStages: Dispatch<SetStateAction<TTestStage[]>>;  questions: TQuestion[];  editQuestions: Dispatch<SetStateAction<TQuestion[]>>;};const initialValues: TTestEditContext = {  baseSettings: mockTesting.baseInfo,  editBaseSettings: () => {},  stages: mockTesting.stages,  editStages: () => {},  questions: mockTesting.questions,  editQuestions: () => {},};const context = createContext<TTestEditContext>({  ...initialValues,});export const useTestEditContext = () => useContext(context) as TTestEditContext;export const TestEditContext: FC<PropsWithChildren> = ({ children }) => {  const [baseSettings, setBaseSettings] = useState<TTestBaseSettings>({    ...initialValues.baseSettings,  });  const [stages, editStages] = useState<TTestStage[]>([    ...initialValues.stages,  ]);  const [questions, editQuestions] = useState<TQuestion[]>([    ...initialValues.questions,  ]);  return (    <context.Provider      value={{        baseSettings: baseSettings,        editBaseSettings: setBaseSettings,        stages,        editStages,        questions,        editQuestions,      }}    >      {children}    </context.Provider>  );};