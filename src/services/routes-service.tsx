import { FC } from 'react';import { BrowserRouter, Route, Routes } from 'react-router-dom';import { Paths } from '../constants/paths';import { BaseLayout } from '../containers/layouts/base-layout';import { HomePage } from '../containers/pages/home-page';import { StatsPage } from '../containers/pages/stats-page';import { TestCreatePage } from '../containers/pages/test-create-page';import { TestEditPage } from '../containers/pages/test-edit-page';import { getTestCreateLink, getTestEditLink } from '../utils/route-utils';export const RoutesService: FC = () => {  return (    <BrowserRouter>      <Routes>        <Route element={<BaseLayout />}>          <Route path={Paths.BASE} element={<HomePage />} />          <Route path={getTestEditLink(':testId')} element={<TestEditPage />} />          <Route path={getTestCreateLink()} element={<TestCreatePage />} />          <Route path={Paths.STATISTIC} element={<StatsPage />} />          <Route path={'*'} element={<HomePage />} />        </Route>      </Routes>    </BrowserRouter>  );};