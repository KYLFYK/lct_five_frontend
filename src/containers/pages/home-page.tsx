import { Button, Tabs } from 'antd';import { FC } from 'react';import { NavLink } from 'react-router-dom';import { CreatedTests } from '../../components/home-page/created-tests';import { ScheduledTests } from '../../components/home-page/scheduled-tests';import { getTestCreateLink } from '../../utils/route-utils';const homeTabs = [  {    key: '1',    label: 'Назначенные',    children: <ScheduledTests />,  },  {    key: '2',    label: 'Созданные',    children: <CreatedTests />,  },];export const HomePage: FC = () => {  return (    <div>      <Tabs        items={homeTabs}        tabBarExtraContent={{          right: (            <NavLink to={getTestCreateLink()}>              <Button type="primary">Создать тест</Button>            </NavLink>          ),        }}      />    </div>  );};